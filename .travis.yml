language: python
branches:
    only: mkdocs

install: 
    - pip install -r requirements.txt

script:
    - mkdocs build -v --clean --strict

deploy:
    provider: pages
    skip_cleanup: true
    github_token: $GITHUB_TOKEN
    local_dir: site
    target-branch: master
    on:
        branch: mkdocs
